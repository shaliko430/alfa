// function stringToBinary(input) {
//     return input
//       .split('') // Split the string into individual characters
//       .map(char => char.charCodeAt(0).toString(2).padStart(8, '0')) // Convert to binary with padding
//       .join(' '); // Join the binary codes with spaces
//   }
  
//   // Example usage
//   const text = "hello";
//   const binary = stringToBinary(text);
  
//   console.log(`Binary code for "${text}":`, binary);


DEVVVVVVV
